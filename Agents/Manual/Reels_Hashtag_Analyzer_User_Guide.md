# 📄 Руководство пользователя: Instagram Reels Hashtag Analyzer

Этот инструмент позволяет находить и анализировать Instagram Reels по заданным **хэштегам**. Он идеально подходит для исследования ниш, поиска трендов и анализа контента конкурентов в определенной тематике.

---

## 🚀 Основные возможности

1. **Поиск по хэштегам**: Находит самые популярные или свежие Reels по вашим ключевым словам.
2. **Глубокий анализ**: Собирает просмотры, лайки, комментарии, длительность и дату публикации.
3. **Транскрибация (Whisper)**: Скачивает видео и переводит голос в текст, выделяя "хуки" (первые фразы).
4. **Google Таблицы**: Сохраняет данные в таблицу в режиме реального времени (строка за строкой).
5. **Отчетность**: Генерирует готовый Markdown-отчет с топом лучших видео и рекомендациями.

---

## 🛠 Подготовка к запуску

Перед использованием убедитесь, что выполнены все требования из [общей инструкции](Reels_Analyzer_User_Guide.md), а именно:

1. Установлен **Python 3.10+** и **FFmpeg**.
2. Установлены зависимости:

    ```bash
    pip install apify-client gspread google-auth google-auth-oauthlib python-dotenv requests openai-whisper yt-dlp
    ```

3. Файл `Credentials.env` содержит `APIFY_API_TOKEN`.
4. Файл `google_credentials.json` находится в папке `Agents/Agents/`.

---

## 💻 Как использовать

Запуск производится через терминал из корневой папки проекта (`Agents/`).

### Базовая команда

```bash
python /Users/akobiyada/Documents/Obsidian/obsidian_cloud_db/Agents/Scripts/reels_analyzer_hashtags.py --hashtags "rwa,blockchain" --limit 50
```

### Доступные параметры

| Параметр | Флаг | Описание | Пример |
| :--- | :--- | :--- | :--- |
| **Хэштеги** (Обязательно) | `--hashtags` или `-t` | Список хэштегов через запятую (без решетки `#`). | `-t "psychology,motivation"` |
| **Лимит** | `--limit` или `-l` | Сколько видео искать (по умолчанию 100). | `-l 50` |
| **Модель Whisper** | `--whisper-model` или `-w` | Размер модели: `tiny`, `base`, `small`, `medium`, `large`. (Default: `base`) | `-w small` |
| **Без транскрибации** | `--skip-transcription` | Если нужно только собрать данные, без скачивания видео и перевода в текст. | `--skip-transcription` |

### Примеры запуска

**1. Простой анализ ниши "crypto":**

```bash
python Agents/Scripts/reels_analyzer_hashtags.py --hashtags "crypto,bitcoin" --limit 30
```

**2. Глубокий анализ с качественной транскрибацией:**

```bash
python Agents/Scripts/reels_analyzer_hashtags.py -t "meditation,yoga" -l 20 -w medium
```

**3. Только сбор статистики (быстро, без скачивания видео):**

```bash
python Agents/Scripts/reels_analyzer_hashtags.py -t "funny,cats" -l 200 --skip-transcription
```

---

## 📊 Результаты работы

После завершения скрипта вы получите:

1. **Google Таблица**:
    * В терминале появится ссылка: `🔗 LINK: https://docs.google.com/...`
    * Вкладка **Raw Data**: Все собранные данные + текст видео.
    * Вкладка **Ranking**: Рейтинг видео по просмотрам.

2. **Markdown Отчет**:
    * Сохраняется в папку `Agents/Executions/`.
    * Имя файла: `[Дата]_reels_analysis_[Хэштеги]_RU.md`.
    * Содержит статистику, ТОП-5 видео и успешные "хуки".

---

## 💡 Особенности работы

* **Инкрементальное сохранение**: Скрипт сохраняет каждую строку в Google Таблицу сразу после обработки. Если скрипт прервется, данные не пропадут.
* **Авторизация Google**: При первом запуске скрипт может открыть браузер и попросить подтвердить доступ к Google (OAuth). Это нормально, подтвердите доступ.
* **Кэш Apify**: Скрипт старается игнорировать старый кэш и искать свежие данные.

---

## ❓ Решение проблем

* **`yt-dlp NOT FOUND`**: Скрипт попытается установить его сам. Если не выйдет — установите вручную: `pip install yt-dlp`.
* **Ошибки скачивания видео**: Некоторые видео могут быть недоступны для скачивания (приватные аккаунты или бан IP). Скрипт пропустит их и пойдет дальше.
* **Google Auth Error**: Если возникают ошибки доступа к таблицам, попробуйте удалить файл `Agents/google_token.pickle` (если он есть) и запустить скрипт снова, чтобы пройти авторизацию заново.
